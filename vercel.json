{
  "version": 2,
  "installCommand": "npm ci && cd frontend && npm ci",
  "buildCommand": "npm run build",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}
